# PortfoLens Frontend Environment Variables
# Copy this file to .env for local development
# For production (Vercel), set these in the Vercel dashboard

# API Base URL - Backend server
# Local development (default if not set in dev mode):
VITE_API_BASE_URL=http://localhost:5000/api

# Production (set this in Vercel Environment Variables):
# VITE_API_BASE_URL=https://portfolens.onrender.com/api
